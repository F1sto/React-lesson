let initialState = {
    friendList: [
        {id: 1, name: 'Bob'},
        {id: 2, name: 'Lee'},
        {id: 3, name: 'John'},
    ],
};

const mainPageReducer = (state = initialState, action) => {
    return state;
}

export default mainPageReducer;