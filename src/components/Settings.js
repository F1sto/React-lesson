import React from 'react';
import classes from '../components-style/Settings.module.css'

const Settings = () => {
    return (
        <div>
            Settings
        </div>
    );
}

export default Settings;